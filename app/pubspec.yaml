name: smart_landmarks2
description: Smart Geo-Tagged Landmarks — CSE489 Midterm (Student 2)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  sqflite: ^2.3.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.0
  geolocator: ^13.0.0
  image_picker: ^1.1.0
  workmanager: ^0.10.0
  connectivity_plus: ^6.0.0
  provider: ^6.1.0
  cached_network_image: ^3.3.0
  path_provider: ^2.1.0
  permission_handler: ^11.3.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
